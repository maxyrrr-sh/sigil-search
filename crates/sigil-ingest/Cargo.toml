[package]
name = "sigil-ingest"
version = "0.0.0"
edition = "2021"
publish = false
description = "Inputs, codecs, template mining (DESIGN section 6)"

[dependencies]
sigil-core = { workspace = true }
serde_json = { workspace = true }
regex = { workspace = true }
anyhow = { workspace = true }

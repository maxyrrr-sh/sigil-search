[package]
name = "siem-alert"
version = "0.0.0"
edition = "2021"
publish = false
description = "Alerting Output plugin for Sigil Search"

[dependencies]
sigil-core = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
